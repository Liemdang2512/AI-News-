---
phase: 01-quy-trinh-doc-va-tom-tat
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - backend/services/summarizer.py
  - backend/prompts.py
autonomous: true
requirements:
  - REQ-002
  - REQ-003

must_haves:
  truths:
    - "Summarizer._has_bullet_content() là staticmethod trả về True/False dùng regex r'^- .{20,}'"
    - "Validation trong _process_single_article dùng _has_bullet_content() thay cho '- ' in summary"
    - "BATCH_SIZE tăng từ 5 lên 10 trong summarize_articles_generator()"
    - "asyncio.sleep(2) giữa batches giảm xuống asyncio.sleep(0.5)"
    - "pytest test_summarizer.py -x -q PASS GREEN (kể cả các test _has_bullet_content)"
    - "Prompt SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT có instruction KHÔNG dùng dấu ngoặc vuông rõ ràng"
  artifacts:
    - path: "backend/services/summarizer.py"
      provides: "_has_bullet_content staticmethod + validation fix + batch tuning"
      contains: "_has_bullet_content"
    - path: "backend/prompts.py"
      provides: "Prompt với explicit no-bracket instruction"
      contains: "KHÔNG dùng dấu ngoặc vuông"
  key_links:
    - from: "backend/services/summarizer.py (_process_single_article)"
      to: "Summarizer._has_bullet_content()"
      via: "replace '- ' in summary with _has_bullet_content(summary)"
      pattern: "_has_bullet_content\\(summary\\)"
    - from: "backend/services/summarizer.py (summarize_articles_generator)"
      to: "BATCH_SIZE constant"
      via: "BATCH_SIZE = 10"
      pattern: "BATCH_SIZE = 10"
---

<objective>
Ba thay đổi orthogonal trong một plan cuối:
1. Thêm _has_bullet_content() staticmethod và fix validation để không bị false-positive từ URL paths
2. Tăng BATCH_SIZE 5→10, giảm inter-batch sleep 2s→0.5s cho throughput tốt hơn
3. Harden SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT với explicit "KHÔNG dùng dấu ngoặc vuông" instruction

Purpose: Đóng REQ-002 (bullet validation chặt hơn) và REQ-003 (prompt format đúng), đồng thời tăng tốc độ xử lý batch.
Output: summarizer.py và prompts.py với các fix trên. pytest test_summarizer.py PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-quy-tr-nh-c-v-t-m-t-t-b-i-vi-t/01-RESEARCH.md

<interfaces>
<!-- Current code trong summarizer.py cần thay đổi — verified từ codebase -->

<!-- 1. Validation hiện tại (line 432 và line 493) — SAI vì "- " match URL path -->
```python
# Line 432 — trong URL context check
if summary and len(summary.strip()) > 150 and "- " in summary:
    return {"category": category.upper(), "text": summary.strip()}

# Line 493 — trong fetch fallback check
if summary and len(summary.strip()) > 150 and "- " in summary:
    return {"category": category.upper(), "text": summary.strip()}

# Line 522 — trong RSS fallback check
if summary and len(summary.strip()) > 100 and "- " in summary:
    return {"category": category.upper(), "text": summary.strip()}
```

<!-- 2. Batch config hiện tại (line 278-312) -->
```python
BATCH_SIZE = 5
# ...
if i + BATCH_SIZE < len(tasks):
    await asyncio.sleep(2)  # <-- giảm xuống 0.5
```

<!-- 3. SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT hiện tại (prompts.py line 148-167) -->
```python
SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT = """Đọc bài báo tại URL sau: {url}
...
- Viết nội dung tin ngắn 130-150 chữ ở đây, bắt đầu bằng dấu gạch ngang này. KHÔNG dùng dấu ngoặc vuông. KHÔNG bỏ dấu gạch ngang đầu dòng.
"""
# Prompt này ĐÃ có "KHÔNG dùng dấu ngoặc vuông" — chỉ cần verify, không cần thay đổi
# Nhưng thiếu "bắt đầu bằng dấu gạch ngang" trong phần instruction rõ ràng
# SINGLE_ARTICLE_SUMMARIZE_PROMPT đã có instruction đúng ở line 194
```

<!-- Fix _has_bullet_content từ research -->
```python
import re

@staticmethod
def _has_bullet_content(summary: str) -> bool:
    """Kiểm tra có ít nhất 1 dòng bullet thực sự (không phải chỉ URL có gạch)."""
    if not summary:
        return False
    return bool(re.search(r'^- .{20,}', summary, re.MULTILINE))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thêm _has_bullet_content() và fix validation trong summarizer.py</name>
  <files>backend/services/summarizer.py</files>
  <read_first>
    - /Users/tanliem/Desktop/App crwal/backend/services/summarizer.py (toàn bộ file — đọc import section, class Summarizer, _process_single_article method đầy đủ)
  </read_first>
  <behavior>
    - _has_bullet_content("### Title\n**Nguồn:** Source\n\n- Đây là nội dung tóm tắt đủ dài hơn 20 ký tự") → True
    - _has_bullet_content("### Title\nhttps://example.com/path-with-dashes") → False
    - _has_bullet_content("- ok") → False (quá ngắn, không đủ 20 chars sau "- ")
    - _has_bullet_content("") → False
    - _has_bullet_content(None) → False (guard)
    - Validation check trong _process_single_article: `len(summary.strip()) > 150 and _has_bullet_content(summary)` thay vì `"- " in summary`
  </behavior>
  <action>
Thực hiện 3 thay đổi trong backend/services/summarizer.py:

**Thay đổi 1: Thêm `import re` nếu chưa có ở đầu file**
File đã có `import re` ở line 3 — không cần thêm.

**Thay đổi 2: Thêm staticmethod `_has_bullet_content` vào class Summarizer**
Thêm method này ngay SAU `_looks_like_block_page` staticmethod (khoảng line 145), trước `_JS_HEAVY_DOMAINS`:

```python
@staticmethod
def _has_bullet_content(summary: str) -> bool:
    """Kiểm tra có ít nhất 1 dòng bullet thực sự (không phải chỉ URL có gạch ngang).

    Dùng regex r'^- .{20,}' multiline thay vì '- ' in summary để tránh false-positive
    với URL paths như https://example.com/path-to-article.
    """
    if not summary:
        return False
    return bool(re.search(r'^- .{20,}', summary, re.MULTILINE))
```

**Thay đổi 3: Thay 3 chỗ dùng `"- " in summary` bằng `Summarizer._has_bullet_content(summary)`**

Tìm và thay trong _process_single_article():

Chỗ 1 — URL context check (khoảng line 432):
```python
# TRƯỚC:
if summary and len(summary.strip()) > 150 and "- " in summary:
# SAU:
if summary and len(summary.strip()) > 150 and Summarizer._has_bullet_content(summary):
```

Chỗ 2 — Fetch fallback check (khoảng line 493):
```python
# TRƯỚC:
if summary and len(summary.strip()) > 150 and "- " in summary:
# SAU:
if summary and len(summary.strip()) > 150 and Summarizer._has_bullet_content(summary):
```

Chỗ 3 — RSS fallback check (khoảng line 522):
```python
# TRƯỚC:
if summary and len(summary.strip()) > 100 and "- " in summary:
# SAU:
if summary and len(summary.strip()) > 100 and Summarizer._has_bullet_content(summary):
```

QUAN TRỌNG: Đọc toàn bộ file trước khi sửa để xác nhận đúng line numbers. Chỉ thay đúng 3 chỗ trên, không thay đổi bất cứ logic nào khác.

Sau khi sửa, chạy:
```bash
cd /Users/tanliem/Desktop/App\ crwal/backend && source venv/bin/activate
python3 -m pytest test_summarizer.py -x -q 2>&1
```
Tất cả tests kể cả test_has_bullet_content_* phải PASS GREEN.
  </action>
  <verify>
    <automated>cd /Users/tanliem/Desktop/App\ crwal/backend && source venv/bin/activate && python3 -m pytest test_summarizer.py -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - pytest test_summarizer.py -x -q exits 0 (tất cả tests PASS, không còn SKIP)
    - grep -n "_has_bullet_content" backend/services/summarizer.py trả về >= 4 dòng (1 definition + 3 call sites)
    - grep -n '"- " in summary' backend/services/summarizer.py trả về 0 dòng (không còn pattern cũ)
    - python3 -c "from services.summarizer import Summarizer; print(Summarizer._has_bullet_content('- test line dài hơn 20 chars OK'))" prints True
  </acceptance_criteria>
  <done>pytest test_summarizer.py exits 0. Tất cả 5 tests PASS (không còn SKIP). "_has_bullet_content" có trong summarizer.py.</done>
</task>

<task type="auto">
  <name>Task 2: Tăng BATCH_SIZE lên 10 và giảm inter-batch sleep xuống 0.5s</name>
  <files>backend/services/summarizer.py</files>
  <read_first>
    - /Users/tanliem/Desktop/App crwal/backend/services/summarizer.py (sau khi đã sửa Task 1 — tìm BATCH_SIZE và asyncio.sleep(2) trong summarize_articles_generator)
  </read_first>
  <action>
Thực hiện 2 thay đổi nhỏ trong summarize_articles_generator() method:

**Thay đổi 1: BATCH_SIZE = 5 → BATCH_SIZE = 10**
Tìm dòng trong summarize_articles_generator():
```python
BATCH_SIZE = 5
```
Thay thành:
```python
BATCH_SIZE = 10
```

**Thay đổi 2: asyncio.sleep(2) → asyncio.sleep(0.5)**
Tìm dòng trong summarize_articles_generator():
```python
await asyncio.sleep(2)
```
(trong block `if i + BATCH_SIZE < len(tasks):`)

Thay thành:
```python
await asyncio.sleep(0.5)
```

QUAN TRỌNG:
- Có 2 chỗ `asyncio.sleep` trong file — một ở đầu _process_single_article (`await asyncio.sleep(0.5)` per-article), một ở summarize_articles_generator (`await asyncio.sleep(2)` inter-batch).
- Chỉ thay inter-batch sleep (trong summarize_articles_generator), KHÔNG thay per-article sleep ở _process_single_article.
- Đọc kỹ context xung quanh dòng sleep trước khi thay để chắc chắn đúng chỗ.

Verify sau khi sửa:
```bash
grep -n "BATCH_SIZE\|asyncio.sleep" backend/services/summarizer.py
```
Phải thấy: BATCH_SIZE = 10, sleep(0.5) ở 2 chỗ (per-article + inter-batch).
  </action>
  <verify>
    <automated>grep -n "BATCH_SIZE\|asyncio.sleep" /Users/tanliem/Desktop/App\ crwal/backend/services/summarizer.py</automated>
  </verify>
  <acceptance_criteria>
    - grep "BATCH_SIZE" trả về dòng chứa `BATCH_SIZE = 10` (không còn `= 5`)
    - grep "asyncio.sleep(2)" trả về 0 dòng trong summarize_articles_generator context
    - grep "asyncio.sleep(0.5)" trả về >= 1 dòng ở inter-batch sleep location
    - python3 -c "from services.summarizer import summarizer" exits 0 (không lỗi import)
    - pytest test_extract.py test_summarizer.py test_prompts.py -x -q vẫn PASS (không regression)
  </acceptance_criteria>
  <done>BATCH_SIZE = 10 visible trong file. Inter-batch sleep là 0.5s. App imports OK.</done>
</task>

<task type="auto">
  <name>Task 3: Verify prompt format và harden SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT nếu cần</name>
  <files>backend/prompts.py</files>
  <read_first>
    - /Users/tanliem/Desktop/App crwal/backend/prompts.py (toàn bộ file — đọc SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT và SINGLE_ARTICLE_SUMMARIZE_PROMPT)
    - /Users/tanliem/Desktop/App crwal/backend/test_prompts.py (xem các assertion cần PASS)
  </read_first>
  <action>
Đọc prompts.py và kiểm tra SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT:

**Kiểm tra các điều kiện sau cho SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT:**
1. Có "KHÔNG dùng dấu ngoặc vuông" — verify từ research, đã có
2. Có "bắt đầu bằng dấu gạch ngang" hoặc tương đương rõ ràng

**Nếu SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT THIẾU instruction về bullet gạch ngang:**
Sửa dòng cuối của prompt (dòng instruction "- Viết nội dung...") để đảm bảo có cả:
- "KHÔNG dùng dấu ngoặc vuông"
- "bắt đầu bằng dấu gạch ngang"

Ví dụ nếu dòng hiện tại là:
```
- Viết nội dung tin ngắn 130-150 chữ ở đây, bắt đầu bằng dấu gạch ngang này. KHÔNG dùng dấu ngoặc vuông. KHÔNG bỏ dấu gạch ngang đầu dòng.
```
Thì ĐÃ ổn — không cần thay đổi gì.

**Nếu thiếu**, thêm vào cuối instruction line:
```
KHÔNG dùng dấu ngoặc vuông. Dòng này bắt đầu bằng dấu gạch ngang (-) là bắt buộc.
```

**Sau khi verify/sửa, chạy:**
```bash
cd /Users/tanliem/Desktop/App\ crwal/backend && source venv/bin/activate
python3 -m pytest test_prompts.py -x -q 2>&1
```
Tất cả 4 tests phải PASS.

**CUỐI CÙNG — Chạy full test suite:**
```bash
cd /Users/tanliem/Desktop/App\ crwal/backend && source venv/bin/activate
python3 -m pytest test_extract.py test_summarizer.py test_prompts.py -v 2>&1
```
Đây là gate cuối cùng cho Phase 1. Mọi tests phải PASS (không còn SKIP nào từ _has_bullet_content).
  </action>
  <verify>
    <automated>cd /Users/tanliem/Desktop/App\ crwal/backend && source venv/bin/activate && python3 -m pytest test_extract.py test_summarizer.py test_prompts.py -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - pytest test_prompts.py -x -q exits 0 (tất cả 4 tests PASS)
    - "KHÔNG dùng dấu ngoặc vuông" tồn tại trong SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT
    - "bắt đầu bằng dấu gạch ngang" hoặc "bắt đầu bằng dấu gạch" tồn tại trong ít nhất 1 trong 2 SINGLE_ARTICLE_*_PROMPT
    - Full suite: python3 -m pytest test_extract.py test_summarizer.py test_prompts.py exits 0
    - Không còn test nào SKIP do AttributeError (plan 03 đã implement _has_bullet_content)
  </acceptance_criteria>
  <done>pytest test_extract.py test_summarizer.py test_prompts.py -v: tất cả tests PASS, 0 failures, 0 skips.</done>
</task>

</tasks>

<verification>
Final phase gate:
```bash
cd /Users/tanliem/Desktop/App\ crwal/backend && source venv/bin/activate && \
  python3 -m pytest test_extract.py test_summarizer.py test_prompts.py -v 2>&1
```

Manual checks:
1. grep "_has_bullet_content" backend/services/summarizer.py — thấy definition + 3 call sites
2. grep '"- " in summary' backend/services/summarizer.py — 0 kết quả
3. grep "BATCH_SIZE" backend/services/summarizer.py — thấy BATCH_SIZE = 10
4. grep "timeout=2000" backend/services/playwright_fetcher.py — thấy dòng wait_for_selector
5. grep "import trafilatura" backend/services/summarizer.py — thấy import block
</verification>

<success_criteria>
REQ-001: _extract_content() dùng trafilatura layer 0, pytest test_extract.py PASS
REQ-001: Playwright wait_for_selector timeout = 2000ms (giảm từ 5000ms)
REQ-002: _has_bullet_content() staticmethod tồn tại, 3 validation checks đã dùng nó
REQ-002: pytest test_summarizer.py PASS (tất cả, không còn SKIP)
REQ-003: pytest test_prompts.py PASS (4 tests)
REQ-003: Prompts có instruction rõ ràng về no-brackets và bullet gạch ngang
Performance: BATCH_SIZE = 10, inter-batch sleep = 0.5s
</success_criteria>

<output>
After completion, create `/Users/tanliem/Desktop/App crwal/.planning/phases/01-quy-tr-nh-c-v-t-m-t-t-b-i-vi-t/01-03-SUMMARY.md`
</output>
