---
phase: 01-quy-trinh-doc-va-tom-tat
plan: "02"
type: execute
wave: 1
depends_on:
  - "01-00"
files_modified:
  - backend/services/playwright_fetcher.py
autonomous: true
requirements:
  - REQ-001

must_haves:
  truths:
    - "wait_for_selector timeout giảm từ 5000ms xuống 2000ms trong PlaywrightFetcher.fetch()"
    - "Worst-case Playwright selector timeout giảm từ 12x5s=60s xuống 12x2s=24s"
    - "App vẫn import và khởi động được sau thay đổi"
    - "Không thay đổi bất cứ logic nào khác trong playwright_fetcher.py"
  artifacts:
    - path: "backend/services/playwright_fetcher.py"
      provides: "Playwright fetcher với timeout tối ưu"
      contains: "timeout=2000"
  key_links:
    - from: "backend/services/playwright_fetcher.py (fetch method)"
      to: "page.wait_for_selector"
      via: "timeout parameter = 2000"
      pattern: "wait_for_selector.*timeout=2000"
---

<objective>
Giảm Playwright wait_for_selector timeout từ 5000ms xuống 2000ms. Đây là thay đổi một dòng, impact cao: trường hợp xấu nhất (không tìm thấy selector nào) giảm từ 60 giây xuống 24 giây mỗi bài.

Purpose: Báo Việt Nam render content trong 1-2s sau DOMContentLoaded. 5000ms timeout mỗi selector là dư thừa và cộng dồn nhanh với 12 selectors.
Output: playwright_fetcher.py với timeout=2000, không thay đổi gì khác.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-quy-tr-nh-c-v-t-m-t-t-b-i-vi-t/01-RESEARCH.md

<interfaces>
<!-- Current playwright_fetcher.py — verified từ codebase -->

```python
# backend/services/playwright_fetcher.py lines 104-110 (current code)
# Thử chờ selector bài báo xuất hiện (tối đa 5s)
for selector in ARTICLE_SELECTORS:
    try:
        await page.wait_for_selector(selector, timeout=5000)  # <-- ĐÂY là dòng cần sửa
        break  # Tìm thấy, dừng
    except Exception:
        continue

ARTICLE_SELECTORS = [  # 12 selectors
    ".fck_detail", ".detail__content", ".cms-body", ".article-body",
    ".content-detail", ".detail-content", ".post-content", ".entry-content",
    "#article-body", "article", '[role="main"]', ".sapo",
]
```

Thay đổi duy nhất: `timeout=5000` → `timeout=2000` (dòng 107)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Giảm wait_for_selector timeout từ 5000ms xuống 2000ms</name>
  <files>backend/services/playwright_fetcher.py</files>
  <read_first>
    - /Users/tanliem/Desktop/App crwal/backend/services/playwright_fetcher.py (toàn bộ file — xác định dòng chứa timeout=5000)
  </read_first>
  <action>
Đây là thay đổi một dòng duy nhất trong file backend/services/playwright_fetcher.py.

Tìm dòng:
```python
await page.wait_for_selector(selector, timeout=5000)
```

Thay thành:
```python
await page.wait_for_selector(selector, timeout=2000)
```

Chú ý:
- Chỉ thay đổi dòng này, không thay đổi bất cứ dòng nào khác trong file
- Comment "tối đa 5s" ở dòng trên cũng nên cập nhật thành "tối đa 2s" cho nhất quán
- Không thêm import, không thay đổi ARTICLE_SELECTORS, không thay đổi logic fetch

Sau khi sửa, verify bằng grep:
```bash
grep -n "wait_for_selector" backend/services/playwright_fetcher.py
```
Phải thấy `timeout=2000`, không còn `timeout=5000`.
  </action>
  <verify>
    <automated>grep -n "wait_for_selector" /Users/tanliem/Desktop/App\ crwal/backend/services/playwright_fetcher.py</automated>
  </verify>
  <acceptance_criteria>
    - grep "wait_for_selector" trong file trả về dòng chứa `timeout=2000`
    - grep "5000" trong playwright_fetcher.py trả về 0 kết quả (không còn timeout=5000)
    - python3 -c "from services.playwright_fetcher import playwright_fetcher" trong venv không lỗi
    - File chỉ có đúng 1 thay đổi so với trước (dòng timeout)
  </acceptance_criteria>
  <done>grep -n "5000" backend/services/playwright_fetcher.py trả về 0 dòng. grep -n "2000" trả về dòng wait_for_selector.</done>
</task>

<task type="auto">
  <name>Task 2: Verify app import và smoke test</name>
  <files></files>
  <read_first>
    - /Users/tanliem/Desktop/App crwal/backend/services/playwright_fetcher.py (sau khi sửa, xác nhận thay đổi đúng)
  </read_first>
  <action>
Chạy các lệnh verify sau để đảm bảo thay đổi không làm hỏng imports:

```bash
cd /Users/tanliem/Desktop/App\ crwal/backend
source venv/bin/activate

# 1. Verify import OK
python3 -c "from services.playwright_fetcher import playwright_fetcher, ARTICLE_SELECTORS; print('Import OK, selectors:', len(ARTICLE_SELECTORS))"

# 2. Verify file không còn timeout=5000
python3 -c "
content = open('services/playwright_fetcher.py').read()
assert 'timeout=5000' not in content, 'Vẫn còn timeout=5000!'
assert 'timeout=2000' in content, 'Thiếu timeout=2000!'
print('PASS: timeout đã được giảm xuống 2000ms')
"

# 3. Chạy pytest test_extract.py để đảm bảo không có regression
python3 -m pytest test_extract.py test_summarizer.py test_prompts.py -x -q 2>&1 | tail -15
```

Nếu bất kỳ lệnh nào fail, đọc lại playwright_fetcher.py và kiểm tra xem có thay đổi sai chỗ không.
  </action>
  <verify>
    <automated>cd /Users/tanliem/Desktop/App\ crwal/backend && source venv/bin/activate && python3 -c "content = open('services/playwright_fetcher.py').read(); assert 'timeout=5000' not in content; assert 'timeout=2000' in content; print('PASS')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "from services.playwright_fetcher import playwright_fetcher"` exits 0
    - File không chứa `timeout=5000`
    - File chứa `timeout=2000`
    - pytest test files vẫn collect OK (không có regression từ playwright_fetcher change)
  </acceptance_criteria>
  <done>python3 inline assertion PASS. Import playwright_fetcher exits 0.</done>
</task>

</tasks>

<verification>
cd /Users/tanliem/Desktop/App\ crwal/backend && grep -n "wait_for_selector\|timeout=5000\|timeout=2000" services/playwright_fetcher.py
</verification>

<success_criteria>
- playwright_fetcher.py: `timeout=5000` không còn tồn tại
- playwright_fetcher.py: `timeout=2000` xuất hiện đúng chỗ wait_for_selector
- App import không lỗi
- Không có thay đổi nào khác ngoài dòng timeout
</success_criteria>

<output>
After completion, create `/Users/tanliem/Desktop/App crwal/.planning/phases/01-quy-tr-nh-c-v-t-m-t-t-b-i-vi-t/01-02-SUMMARY.md`
</output>
