---
phase: 01-quy-trinh-doc-va-tom-tat
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/test_extract.py
  - backend/test_summarizer.py
  - backend/test_prompts.py
  - backend/fixtures/laodong_article.html
  - backend/fixtures/dantri_article.html
autonomous: true
requirements:
  - REQ-001
  - REQ-002
  - REQ-003

must_haves:
  truths:
    - "pytest backend/test_extract.py -x exits 0 (tests run, may fail RED before implementation)"
    - "pytest backend/test_summarizer.py -x exits 0 (tests collected)"
    - "pytest backend/test_prompts.py -x exits 0 (tests collected)"
    - "HTML fixtures exist and contain realistic Vietnamese article HTML"
  artifacts:
    - path: "backend/test_extract.py"
      provides: "Unit tests for _extract_content() covering REQ-001"
      exports: ["test_extract_laodong", "test_extract_dantri", "test_extract_returns_200_chars"]
    - path: "backend/test_summarizer.py"
      provides: "Unit tests for bullet validation and fallback logic covering REQ-002"
      exports: ["test_fallback_has_bullet", "test_bullet_validation_rejects_url_dash"]
    - path: "backend/test_prompts.py"
      provides: "Tests that prompts do not produce placeholder brackets in output format"
      exports: ["test_single_article_prompt_no_brackets", "test_url_prompt_no_brackets"]
    - path: "backend/fixtures/laodong_article.html"
      provides: "Realistic laodong.vn HTML with article body"
      contains: "fck_detail"
    - path: "backend/fixtures/dantri_article.html"
      provides: "Realistic dantri.com.vn HTML with article body"
      contains: "detail__content"
  key_links:
    - from: "backend/test_extract.py"
      to: "backend/services/summarizer.py"
      via: "sys.path + import Summarizer"
      pattern: "from services.summarizer import Summarizer"
    - from: "backend/test_summarizer.py"
      to: "backend/services/summarizer.py"
      via: "import Summarizer._has_bullet_content (after plan 03 adds it)"
      pattern: "_has_bullet_content"
---

<objective>
Create test scaffolds (HTML fixtures + test files) that verify all three requirements.
Tests are written BEFORE implementation so they run RED now and go GREEN after plans 01-03.

Purpose: Satisfy Nyquist rule — every subsequent task must have an automated verify command.
Output: Three test files + two HTML fixtures in backend/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-quy-tr-nh-c-v-t-m-t-t-b-i-vi-t/01-RESEARCH.md
</context>

<interfaces>
<!-- Key signatures from backend/services/summarizer.py (read before writing tests) -->

```python
class Summarizer:
    def _extract_content(self, html: str, limit: int = 8000) -> str: ...
    # Returns extracted text string, or "" on failure

    @staticmethod
    def _has_bullet_content(summary: str) -> bool: ...
    # DOES NOT EXIST YET — plan 03 will add it.
    # Test should import it and expect it to work after plan 03.

    def get_fallback_summary(note: str = "") -> str: ...
    # Actually defined as inner function in _process_single_article.
    # Test via _excerpt_only_fallback() which is a staticmethod and always produces "- "

    @staticmethod
    def _excerpt_only_fallback(title, url, source, content, max_len=520) -> str:
        # Always includes "- {excerpt}" bullet — test this as proxy for fallback behavior
```

From backend/prompts.py:
```python
SINGLE_ARTICLE_SUMMARIZE_PROMPT  # has {title}, {source}, {url}, {content} placeholders
SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT  # has {url}, {title}, {source} placeholders
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create HTML fixtures for Vietnamese news sites</name>
  <files>backend/fixtures/laodong_article.html, backend/fixtures/dantri_article.html</files>
  <read_first>
    - /Users/tanliem/Desktop/App crwal/backend/services/summarizer.py (lines 533-590, _extract_content selectors)
    - /Users/tanliem/Desktop/App crwal/backend/services/playwright_fetcher.py (lines 16-20, ARTICLE_SELECTORS)
  </read_first>
  <action>
    Create backend/fixtures/ directory and two minimal but realistic HTML files.

    backend/fixtures/laodong_article.html:
    - Valid HTML5 structure with <head> and <body>
    - Include a <div class="fck_detail"> containing >= 500 chars of dummy Vietnamese text
    - Include JSON-LD <script type="application/ld+json"> with "@type": "NewsArticle" and "articleBody" containing >= 300 chars
    - Include <meta property="og:description" content="Mô tả ngắn bài viết laodong...">
    - The fck_detail div text should be a block of realistic Vietnamese sentences totaling >= 500 chars
    - Example content: "Bộ Tài chính vừa công bố kết quả thu ngân sách nhà nước tháng 3 năm 2026..." (continue for >= 500 chars total)

    backend/fixtures/dantri_article.html:
    - Valid HTML5 structure
    - Include a <div class="detail__content"> containing >= 500 chars of dummy Vietnamese text
    - Include JSON-LD with "@type": "NewsArticle" and "articleBody" >= 300 chars
    - Include <meta name="description" content="Mô tả ngắn bài viết dantri...">
    - The detail__content div text >= 500 chars

    Both files: add <script> and <nav> tags with junk text (to test that _extract_content strips them properly).
  </action>
  <verify>
    <automated>python3 -c "
import os
assert os.path.exists('backend/fixtures/laodong_article.html'), 'laodong fixture missing'
assert os.path.exists('backend/fixtures/dantri_article.html'), 'dantri fixture missing'
with open('backend/fixtures/laodong_article.html') as f:
    c = f.read()
assert 'fck_detail' in c, 'laodong: missing fck_detail selector'
assert len(c) > 600, f'laodong fixture too short: {len(c)}'
with open('backend/fixtures/dantri_article.html') as f:
    c = f.read()
assert 'detail__content' in c, 'dantri: missing detail__content selector'
assert len(c) > 600, f'dantri fixture too short: {len(c)}'
print('PASS: fixtures OK')
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - backend/fixtures/laodong_article.html exists, contains "fck_detail", total file > 600 chars
    - backend/fixtures/dantri_article.html exists, contains "detail__content", total file > 600 chars
    - Both files parse as valid HTML (BeautifulSoup does not throw)
    - Each fixture contains at least one article content div with >= 500 chars of Vietnamese text
  </acceptance_criteria>
  <done>Both fixture files exist and pass the python3 inline check above without AssertionError.</done>
</task>

<task type="auto">
  <name>Task 2: Write unit test files for REQ-001, REQ-002, REQ-003</name>
  <files>backend/test_extract.py, backend/test_summarizer.py, backend/test_prompts.py</files>
  <read_first>
    - /Users/tanliem/Desktop/App crwal/backend/services/summarizer.py (full file — understand _extract_content, _excerpt_only_fallback)
    - /Users/tanliem/Desktop/App crwal/backend/prompts.py (lines 145-200 — SINGLE_ARTICLE_SUMMARIZE_PROMPT, SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT)
    - /Users/tanliem/Desktop/App crwal/backend/test_api_v2.py (to understand existing test pattern and sys.path setup)
  </read_first>
  <action>
    All three test files are run from the backend/ directory. Use `sys.path.insert(0, os.path.dirname(__file__))` at top.

    --- backend/test_extract.py ---
    Tests for _extract_content() covering REQ-001.
    Import: `import sys, os; sys.path.insert(0, os.path.dirname(__file__))` then `from services.summarizer import Summarizer`

    Tests to write:
    1. test_extract_laodong_selector: Load backend/fixtures/laodong_article.html, call Summarizer()._extract_content(html), assert len(result) >= 200
    2. test_extract_dantri_selector: Same with dantri fixture, assert len(result) >= 200
    3. test_extract_empty_html: Call with "", assert result == ""
    4. test_extract_json_ld: Pass HTML with only a JSON-LD script block containing articleBody of 300+ chars, assert len(result) >= 200
    5. test_extract_meta_fallback: Pass HTML with only og:description meta (50 chars), assert result contains that text

    NOTE: Tests 1 and 2 will run RED (fail) before plan 01 adds trafilatura. They should still be importable and collected by pytest. Use fixtures relative path: `os.path.join(os.path.dirname(__file__), "fixtures", "laodong_article.html")`.

    --- backend/test_summarizer.py ---
    Tests for bullet validation and fallback covering REQ-002.
    Import Summarizer. After plan 03, _has_bullet_content will exist on Summarizer.

    Tests to write:
    1. test_excerpt_only_fallback_has_bullet: Call Summarizer._excerpt_only_fallback("Title", "http://x.com/path", "Source", "Some content " * 10), assert "- " in result and result.startswith("### [")
    2. test_excerpt_only_fallback_empty_content: Call with content="", assert "- " in result (should show default message)
    3. test_has_bullet_valid: (WILL FAIL RED before plan 03) Call Summarizer._has_bullet_content("### Title\n**Src**\n\n- Đây là nội dung tóm tắt bài viết đầy đủ"), assert result is True
    4. test_has_bullet_url_dash_rejected: (WILL FAIL RED before plan 03) Call Summarizer._has_bullet_content("### Title\nhttps://example.com/path-to-article"), assert result is False
    5. test_has_bullet_short_bullet_rejected: Call Summarizer._has_bullet_content("- ok"), assert result is False (fewer than 20 chars after "- ")

    For tests that call a method that does not exist yet (_has_bullet_content), wrap them in try/except AttributeError so pytest can collect them but they skip gracefully:
    ```python
    def test_has_bullet_valid():
        try:
            result = Summarizer._has_bullet_content("### Title\n**Src**\n\n- Đây là nội dung tóm tắt bài viết dài hơn 20 chars")
            assert result is True
        except AttributeError:
            pytest.skip("_has_bullet_content not yet implemented (plan 03)")
    ```

    --- backend/test_prompts.py ---
    Tests for prompt format covering REQ-003.
    Import: from prompts import SINGLE_ARTICLE_SUMMARIZE_PROMPT, SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT

    Tests to write:
    1. test_single_article_prompt_no_brackets: Format SINGLE_ARTICLE_SUMMARIZE_PROMPT with sample values, assert "[" not in the instruction lines (the KHÔNG dùng dấu ngoặc vuông instructions should be there), assert "- Viết nội dung" in formatted_prompt
    2. test_url_prompt_no_brackets: Format SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT with url="https://example.com/bai-viet", title="Tiêu đề test", source="Nguồn test", assert "KHÔNG dùng dấu ngoặc vuông" in formatted_prompt
    3. test_single_article_prompt_has_bullet_instruction: assert "bắt đầu bằng dấu gạch ngang" in SINGLE_ARTICLE_SUMMARIZE_PROMPT
    4. test_url_prompt_has_bullet_instruction: assert "bắt đầu bằng dấu gạch ngang" in SINGLE_ARTICLE_URL_SUMMARIZE_PROMPT

    Each test file: add `if __name__ == "__main__": pytest.main([__file__, "-v"])` at bottom.
  </action>
  <verify>
    <automated>cd /Users/tanliem/Desktop/App\ crwal/backend && python3 -m pytest test_extract.py test_summarizer.py test_prompts.py --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - pytest --collect-only finds at least 5 tests in test_extract.py, 5 in test_summarizer.py, 4 in test_prompts.py
    - No ImportError or SyntaxError during collection
    - test_prompts.py tests all PASS immediately (they test existing prompt strings)
    - test_summarizer.py tests 1 and 2 (test_excerpt_only_fallback_*) PASS immediately
    - test_summarizer.py tests 3-5 (_has_bullet_content) SKIP gracefully with AttributeError guard
    - test_extract.py tests may FAIL or SKIP — that is acceptable at Wave 0
  </acceptance_criteria>
  <done>cd backend && python3 -m pytest test_extract.py test_summarizer.py test_prompts.py --collect-only -q runs without ImportError. test_prompts.py has 4+ passing tests. No collection errors.</done>
</task>

</tasks>

<verification>
cd /Users/tanliem/Desktop/App crwal/backend && python3 -m pytest test_prompts.py test_summarizer.py -x -q 2>&1 | tail -15
</verification>

<success_criteria>
- backend/fixtures/laodong_article.html and dantri_article.html exist with correct selectors
- pytest collects all three test files without error
- test_prompts.py: 4 tests PASS
- test_summarizer.py: tests 1-2 PASS, tests 3-5 SKIP (not fail)
- test_extract.py: collected, may fail RED (acceptable — implementation comes in plan 01)
</success_criteria>

<output>
After completion, create `/Users/tanliem/Desktop/App crwal/.planning/phases/01-quy-tr-nh-c-v-t-m-t-t-b-i-vi-t/01-00-SUMMARY.md`
</output>
